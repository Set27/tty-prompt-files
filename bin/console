#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "tty/files"
require "tty/prompt"

prompt = TTY::Prompt.new

# Drop into an IRB session with the current binding
require "irb"
binding.irb
